// nunca criar constantes com palavras reservadas

// criar constantes com nomes significativos

// nunca criar constantes começadas com nºs

// utilizar letras minúsculas

//não pode conter espaços ou traços

//nome composto utilzamos camelCase

// constantes são Case-sensitive

// não podemos modificar o valor das constantes

// não utiliza var, utilize const

const nome = 'João';
console.log(nome);

// a constante precisar ser inicializada, ou seja,
// colocamos o valor ao declará-la

// não tem como alterar o valor da constante

const numberOne = 5;
const numberTwo = 10;
const conta = numberOne * numberTwo;
let resultado = conta * 3;
resultado = resultado + 5;
console.log(resultado);

console.log(typeof (numberOne));
// descobre o tipo da variável numbreOne

//para concatenar devemos utilizar + com uma string, se for nºs o js soma
